# Полная инструкция по переводу бота на Webhook и настройке календарного сервера на Railway

Этот документ описывает процесс перевода существующего Telegram бота на Railway с polling на webhook и настройки календарного сервера для Apple Calendar.

## 📋 Содержание

1. [Обзор архитектуры](#обзор-архитектуры)
2. [Шаг 1: Обновление существующего worker бота](#шаг-1-обновление-существующего-worker-бота)
3. [Шаг 2: Развёртывание календарного сервера](#шаг-2-развёртывание-календарного-сервера)
4. [Шаг 3: Настройка переменных окружения](#шаг-3-настройка-переменных-окружения)
5. [Шаг 4: Проверка работы](#шаг-4-проверка-работы)
6. [Устранение проблем](#устранение-проблем)

---

## 🏗 Обзор архитектуры

У вас уже есть на Railway:
- ✅ **bot worker** - основной бот (сейчас работает через polling)
- ✅ **admin worker** - админ-панель
- ✅ **Postgres** - база данных

Нужно:
1. **Обновить bot worker** - перевести с polling на webhook
2. **Добавить Calendar Server** - новый сервис для отдачи `.ics` файлов (Apple Calendar)

Все сервисы используют одну и ту же базу данных PostgreSQL.

---

## 🤖 Шаг 1: Обновление существующего worker бота

### 1.1 Обновление кода

1. Убедитесь, что вы закоммитили и запушили все изменения (обновлённый `main.py` с webhook)
2. Railway автоматически начнёт новый деплой после пуша в репозиторий

### 1.2 Настройка переменных окружения

В настройках вашего **bot worker** сервиса:

1. Перейдите в **"Variables"**
2. Добавьте/обновите следующие переменные:

```env
# Обязательные (убедитесь, что они уже есть)
BOT_TOKEN=ваш_токен_telegram_бота
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Новые переменные для webhook
WEBHOOK_HOST=  # Пока оставьте пустым, заполним после деплоя
WEBHOOK_PATH=/webhook
WEBHOOK_SECRET=  # Опционально, для безопасности (сгенерируйте: python3 -c "import secrets; print(secrets.token_urlsafe(32))")

# Для календаря (пока оставьте пустым, заполним после настройки календарного сервера)
CALENDAR_SERVER_URL=

# Существующие переменные (если есть)
ADMIN_USER_IDS=123456789,987654321
VIDEO_FILE_ID=
DEBUG=False
```

### 1.3 Создание публичного домена для бота

1. В настройках **bot worker** перейдите в **"Settings"** → **"Networking"**
2. Найдите раздел **"Public Domain"** или нажмите **"Generate Domain"**
3. Railway создаст публичный URL (например: `https://bot-worker-production.up.railway.app`)
4. Скопируйте этот URL

### 1.4 Обновление WEBHOOK_HOST

1. Вернитесь в **"Variables"**
2. Обновите `WEBHOOK_HOST` на скопированный URL:
   ```
   WEBHOOK_HOST=https://bot-worker-production.up.railway.app
   ```
   **⚠️ Важно:** URL должен быть без слеша в конце!

3. Railway автоматически перезапустит сервис

### 1.5 Проверка webhook

1. Дождитесь завершения деплоя
2. Откройте логи **bot worker**
3. Должны увидеть:
   ```
   Webhook установлен: https://bot-worker-production.up.railway.app/webhook
   Веб-сервер успешно запущен
   ```
4. Откройте в браузере: `https://your-bot-worker-url.railway.app/health`
5. Должно вернуться: `OK`

### 1.6 Проверка работы бота

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Бот должен ответить (теперь через webhook, а не polling!)

---

## 📅 Шаг 2: Развёртывание календарного сервера

### 2.1 Создание второго сервиса

1. В том же проекте нажмите **"New"** → **"GitHub Repo"**
2. Выберите **тот же репозиторий** (да, можно использовать один репозиторий для нескольких сервисов)
3. Railway создаст новый сервис

### 2.2 Настройка календарного сервера

1. Откройте настройки нового сервиса
2. Перейдите в **"Settings"** → **"Deploy"**
3. Убедитесь, что **"Root Directory"** пустой (корень проекта)
4. **Важно:** В поле **"Start Command"** укажите команду для запуска календарного сервера:
   ```
   gunicorn calendar_server:app --bind 0.0.0.0:$PORT --workers 1 --threads 8
   ```
   
   **Почему это нужно:** В вашем `Procfile` указана команда для основного бота (`python main.py`). Railway позволяет указать разные команды запуска для разных сервисов через настройки.

5. В разделе **"Settings"** → **"Networking"** создайте публичный домен (например: `https://wedding-calendar-production.up.railway.app`)

### 2.3 Проверка календарного сервера

1. Дождитесь завершения деплоя
2. Откройте в браузере: `https://wedding-calendar.up.railway.app/wedding.ics`
3. Браузер должен предложить скачать файл `.ics` или открыть его в календаре

---

## ⚙️ Шаг 3: Настройка переменных окружения

### 3.1 Обновление переменных в bot worker

Вернитесь в настройки **bot worker** и обновите:

```env
# Добавьте URL календарного сервера (после того, как создадите календарный сервер)
CALENDAR_SERVER_URL=https://wedding-calendar-production.up.railway.app
```

### 3.2 Полный список переменных для bot worker

```env
# Telegram
BOT_TOKEN=ваш_токен_telegram_бота

# Database
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Webhook
WEBHOOK_HOST=https://bot-worker-production.up.railway.app
WEBHOOK_PATH=/webhook
WEBHOOK_SECRET=ваш_секретный_ключ  # Опционально, сгенерируйте: python3 -c "import secrets; print(secrets.token_urlsafe(32))"

# Calendar
CALENDAR_SERVER_URL=https://wedding-calendar-production.up.railway.app

# Optional
ADMIN_USER_IDS=123456789,987654321
VIDEO_FILE_ID=ваш_file_id_видео
DEBUG=False
```

**Примечание:** Переменные для админ-панели (ADMIN_USERNAME, ADMIN_PASSWORD, SECRET_KEY) остаются в **admin worker**, их не нужно менять.

### 3.3 Переменные для календарного сервера

Календарный сервер не требует дополнительных переменных окружения (все данные хардкожены в `calendar_server.py`).

---

## ✅ Шаг 4: Проверка работы

### 4.1 Проверка бота

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением
4. Проверьте все разделы меню

### 4.2 Проверка календаря

1. Откройте раздел **"💍 Информация о свадьбе"**
2. Должны появиться две кнопки:
   - **📱 Apple Calendar** - для iOS/macOS
   - **📅 Google Calendar** - для Android/всех платформ
3. Нажмите на **"📱 Apple Calendar"** - должно открыться приложение календаря с предзаполненным событием
4. Нажмите на **"📅 Google Calendar"** - должно открыться в браузере с формой добавления события

### 4.3 Проверка логов

В логах обоих сервисов не должно быть ошибок. Если есть ошибки, см. раздел [Устранение проблем](#устранение-проблем).

---

## 🔧 Устранение проблем

### Проблема: "WEBHOOK_HOST не установлен"

**Решение:**
- Убедитесь, что `WEBHOOK_HOST` указан в переменных окружения
- URL должен начинаться с `https://`
- Не должно быть слеша в конце (например: `https://your-app.railway.app`, а не `https://your-app.railway.app/`)

### Проблема: "Webhook установлен, но бот не отвечает"

**Решение:**
1. Проверьте, что webhook действительно установлен:
   - Откройте логи бота
   - Должна быть строка: `Webhook установлен: https://...`
2. Проверьте доступность URL:
   - Откройте `https://your-bot-url.railway.app/health` в браузере
   - Должно вернуться `OK`
3. Проверьте, что бот не запущен локально (конфликт webhook)

### Проблема: "inline keyboard button URL '/wedding.ics' is invalid"

**Решение:**
- Убедитесь, что `CALENDAR_SERVER_URL` установлен в переменных окружения бота
- URL должен быть полным: `https://wedding-calendar.up.railway.app`
- Проверьте, что календарный сервер доступен: откройте `https://your-calendar-url.railway.app/wedding.ics` в браузере

### Проблема: Календарный сервер не отдаёт файл

**Решение:**
1. Проверьте логи календарного сервера
2. Убедитесь, что команда запуска правильная:
   ```
   gunicorn calendar_server:app --bind 0.0.0.0:$PORT
   ```
3. Проверьте, что файл `calendar_server.py` присутствует в репозитории
4. Убедитесь, что `flask` и `gunicorn` установлены (есть в `requirements.txt`)

### Проблема: Бот работает, но календарные кнопки не появляются

**Решение:**
- Проверьте, что `CALENDAR_SERVER_URL` установлен и доступен
- Если URL не установлен, кнопка Apple Calendar не будет показана (это нормально)
- Кнопка Google Calendar должна работать всегда

### Проблема: Railway не может найти Procfile

**Решение:**
- Убедитесь, что `Procfile` присутствует в корне репозитория
- Для календарного сервера можно указать команду запуска вручную в настройках Railway

---

## 📝 Чек-лист перед публикацией

- [ ] Бот развёрнут на Railway и работает через webhook
- [ ] Календарный сервер развёрнут и отдаёт `.ics` файл
- [ ] `WEBHOOK_HOST` установлен и указывает на правильный URL бота
- [ ] `CALENDAR_SERVER_URL` установлен и указывает на правильный URL календарного сервера
- [ ] Все переменные окружения заполнены
- [ ] Бот отвечает на команды в Telegram
- [ ] Кнопки календаря работают корректно
- [ ] Health check endpoints доступны (`/health` для бота, `/wedding.ics` для календаря)
- [ ] Логи не содержат критических ошибок

---

## 🎉 Готово!

После выполнения всех шагов ваш бот должен полностью работать на Railway через webhook, а календарные кнопки должны корректно добавлять событие в календарь пользователей.

Если возникнут проблемы, проверьте логи обоих сервисов на Railway и убедитесь, что все переменные окружения установлены правильно.
