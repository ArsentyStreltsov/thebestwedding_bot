# Telegram Bot для свадьбы

Бот для общения с гостями свадьбы, управления виш-листом и предоставления полезной информации.

## Функционал

- 🎁 **Виш-лист** - просмотр подарков, отметка забранных товаров
- ℹ️ **Полезная информация** - информация о времени росписи, ЗАГСе, маршруте и т.д.
- 📞 **Связь с организаторами** - контакты для связи

## Быстрый старт

### Автоматическая настройка

Запустите скрипт для первоначальной настройки:
```bash
./setup.sh
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Создаст файл `.env` с шаблоном

После этого отредактируйте `.env` файл и укажите:
- `BOT_TOKEN` - токен вашего Telegram бота (получить у @BotFather)
- `DATABASE_URL` - строка подключения к PostgreSQL
- `ADMIN_USER_IDS` - ID администраторов через запятую (опционально)

### Ручная установка

1. Клонируйте репозиторий или скачайте файлы

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip3 install -r requirements.txt
```

4. Создайте файл `.env` и заполните его (см. `.env.example`)

## Запуск

### Локальный запуск (рекомендуется)

Используйте скрипт для запуска с проверками:
```bash
./run_local.sh
```

Скрипт автоматически:
- Проверит наличие `.env` файла
- Проверит настройки токена и БД
- Активирует виртуальное окружение
- Установит зависимости при необходимости
- Проверит подключение к базе данных
- Запустит бота

### Ручной запуск

```bash
python3 main.py
```

## База данных

Бот использует PostgreSQL. При первом запуске автоматически создаются необходимые таблицы:
- `users` - пользователи бота
- `wishlist_items` - товары виш-листа
- `wedding_info` - полезная информация
- `quizzes` - викторины (для будущего функционала)
- `quiz_questions` - вопросы викторин
- `quiz_answers` - ответы пользователей

## Структура проекта

```
thebestwedding_bot/
├── main.py              # Точка входа
├── config.py            # Конфигурация
├── database/            # Работа с БД
│   ├── connection.py
│   └── models.py
├── handlers/            # Обработчики сообщений
│   ├── start.py
│   ├── wishlist.py
│   ├── info.py
│   └── contact.py
├── keyboards/           # Клавиатуры
│   ├── main_menu.py
│   ├── wishlist.py
│   └── info.py
├── setup.sh             # Скрипт первоначальной настройки
├── run_local.sh         # Скрипт локального запуска
├── deploy.sh            # Скрипт деплоя на GitHub
├── requirements.txt
└── README.md
```

## Деплой на GitHub

Для отправки изменений на GitHub используйте скрипт:
```bash
./deploy.sh "описание изменений"
```

Или без указания сообщения (скрипт попросит ввести):
```bash
./deploy.sh
```

Скрипт автоматически:
- Проверит наличие `.env` файла (чтобы не закоммитить его случайно)
- Покажет статус изменений
- Добавит все файлы
- Создаст коммит
- Отправит изменения на GitHub

⚠️ **Важно**: Скрипт не закоммитит `.env` файл, так как он в `.gitignore`

## Развертывание на Railway

1. Подключите репозиторий к Railway
2. **Важно**: Файл `runtime.txt` уже настроен для использования Python 3.12 (совместим с asyncpg и pydantic)
3. Добавьте переменные окружения в настройках проекта:
   - `BOT_TOKEN` - токен вашего Telegram бота
   - `DATABASE_URL` - используйте Variable Reference: `${{Postgres.DATABASE_URL}}` (рекомендуется)
     или скопируйте обычный `DATABASE_URL` (не `DATABASE_PUBLIC_URL`) из сервиса Postgres
   - `ADMIN_USER_IDS` - ID администраторов через запятую (опционально)
   - `DEBUG=False`
4. Railway автоматически определит Python проект и установит зависимости
5. Убедитесь, что PostgreSQL добавлен как сервис
6. При первом запуске таблицы создадутся автоматически

**Примечание**: Если возникают проблемы с билдом, убедитесь, что файл `runtime.txt` присутствует в репозитории и содержит `python-3.12.7`

## Безопасность

⚠️ **Важно**: Никогда не коммитьте файл `.env` в репозиторий! Он уже добавлен в `.gitignore`.
